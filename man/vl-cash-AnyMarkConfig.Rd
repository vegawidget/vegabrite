% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzz_autogen_api.R
\name{vl$AnyMarkConfig}
\alias{vl$AnyMarkConfig}
\title{AnyMarkConfig}
\arguments{
\item{align}{(\emph{MarkConfig<>, AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig}) The horizontal alignment of the text or ranged marks (area, bar, image, rect, rule). One of \code{"left"}, \code{"right"}, \code{"center"}.

\strong{Note:} Expression reference is \emph{not} supported for range marks.}

\item{angle}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The rotation angle of the text, in degrees.}

\item{aria}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) A boolean flag indicating if \href{https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA}{ARIA attributes} should be included (SVG output only). If \code{false}, the "aria-hidden" attribute will be set on the output SVG element, removing the mark item from the ARIA accessibility tree.}

\item{ariaRole}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) Sets the type of user interface element of the mark item for \href{https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA}{ARIA accessibility} (SVG output only). If specified, this property determines the "role" attribute. Warning: this property is experimental and may be changed in the future.}

\item{ariaRoleDescription}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) A human-readable, author-localized description for the role of the mark item for \href{https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA}{ARIA accessibility} (SVG output only). If specified, this property determines the "aria-roledescription" attribute. Warning: this property is experimental and may be changed in the future.}

\item{aspect}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) Whether to keep aspect ratio of image marks.}

\item{baseline}{(\emph{MarkConfig<>, AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig}) For text marks, the vertical text baseline. One of \code{"alphabetic"} (default), \code{"top"}, \code{"middle"}, \code{"bottom"}, \code{"line-top"}, \code{"line-bottom"}, or an expression reference that provides one of the valid values. The \code{"line-top"} and \code{"line-bottom"} values operate similarly to \code{"top"} and \code{"bottom"}, but are calculated relative to the \code{lineHeight} rather than \code{fontSize} alone.

For range marks, the vertical alignment of the marks. One of \code{"top"}, \code{"middle"}, \code{"bottom"}.

\strong{Note:} Expression reference is \emph{not} supported for range marks.}

\item{blend}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The color blend mode for drawing an item on its current background. Any valid \href{https://developer.mozilla.org/en-US/docs/Web/CSS/mix-blend-mode}{CSS mix-blend-mode} value can be used.

__Default value: \code{"source-over"}}

\item{color}{(\emph{MarkConfig<>, AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig}) Default color.

\strong{Default value:} <span style="color: #4682b4;">■</span> \code{"#4682b4"}

\strong{Note:} - This property cannot be used in a \href{https://vega.github.io/vega-lite/docs/mark.html#style-config}{style config}. - The \code{fill} and \code{stroke} properties have higher precedence than \code{color} and will override \code{color}.}

\item{cornerRadius}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The radius in pixels of rounded rectangles or arcs' corners.

\strong{Default value:} \code{0}}

\item{cornerRadiusBottomLeft}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The radius in pixels of rounded rectangles' bottom left corner.

\strong{Default value:} \code{0}}

\item{cornerRadiusBottomRight}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The radius in pixels of rounded rectangles' bottom right corner.

\strong{Default value:} \code{0}}

\item{cornerRadiusTopLeft}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The radius in pixels of rounded rectangles' top right corner.

\strong{Default value:} \code{0}}

\item{cornerRadiusTopRight}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The radius in pixels of rounded rectangles' top left corner.

\strong{Default value:} \code{0}}

\item{cursor}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The mouse cursor used over the mark. Any valid \href{https://developer.mozilla.org/en-US/docs/Web/CSS/cursor#Values}{CSS cursor type} can be used.}

\item{description}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) A text description of the mark item for \href{https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA}{ARIA accessibility} (SVG output only). If specified, this property determines the \href{https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/ARIA_Techniques/Using_the_aria-label_attribute}{"aria-label" attribute}.}

\item{dir}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The direction of the text. One of \code{"ltr"} (left-to-right) or \code{"rtl"} (right-to-left). This property determines on which side is truncated in response to the limit parameter.

\strong{Default value:} \code{"ltr"}}

\item{dx}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The horizontal offset, in pixels, between the text label and its anchor point. The offset is applied after rotation by the \emph{angle} property.}

\item{dy}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The vertical offset, in pixels, between the text label and its anchor point. The offset is applied after rotation by the \emph{angle} property.}

\item{ellipsis}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The ellipsis string for text truncated in response to the limit parameter.

\strong{Default value:} \code{"…"}}

\item{endAngle}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The end angle in radians for arc marks. A value of \code{0} indicates up (north), increasing values proceed clockwise.}

\item{fill}{(\emph{MarkConfig<>, AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig}) Default fill color. This property has higher precedence than \code{config.color}. Set to \code{null} to remove fill.

\strong{Default value:} (None)}

\item{fillOpacity}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The fill opacity (value between [0,1]).

\strong{Default value:} \code{1}}

\item{filled}{(\emph{MarkConfig<>, AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig}) Whether the mark's color should be used as fill color instead of stroke color.

\strong{Default value:} \code{false} for all \code{point}, \code{line}, and \code{rule} marks as well as \code{geoshape} marks for \href{https://vega.github.io/vega-lite/docs/data.html#graticule}{\code{graticule}} data sources; otherwise, \code{true}.

\strong{Note:} This property cannot be used in a \href{https://vega.github.io/vega-lite/docs/mark.html#style-config}{style config}.}

\item{font}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The typeface to set the text in (e.g., \code{"Helvetica Neue"}).}

\item{fontSize}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The font size, in pixels.

\strong{Default value:} \code{11}}

\item{fontStyle}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The font style (e.g., \code{"italic"}).}

\item{fontWeight}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The font weight. This can be either a string (e.g \code{"bold"}, \code{"normal"}) or a number (\code{100}, \code{200}, \code{300}, ..., \code{900} where \code{"normal"} = \code{400} and \code{"bold"} = \code{700}).}

\item{height}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) Height of the marks.}

\item{href}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) A URL to load upon mouse click. If defined, the mark acts as a hyperlink.}

\item{innerRadius}{(\emph{MarkConfig<>, AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig}) The inner radius in pixels of arc marks. \code{innerRadius} is an alias for \code{radius2}.}

\item{interpolate}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The line interpolation method to use for line and area marks. One of the following: - \code{"linear"}: piecewise linear segments, as in a polyline. - \code{"linear-closed"}: close the linear segments to form a polygon. - \code{"step"}: alternate between horizontal and vertical segments, as in a step function. - \code{"step-before"}: alternate between vertical and horizontal segments, as in a step function. - \code{"step-after"}: alternate between horizontal and vertical segments, as in a step function. - \code{"basis"}: a B-spline, with control point duplication on the ends. - \code{"basis-open"}: an open B-spline; may not intersect the start or end. - \code{"basis-closed"}: a closed B-spline, as in a loop. - \code{"cardinal"}: a Cardinal spline, with control point duplication on the ends. - \code{"cardinal-open"}: an open Cardinal spline; may not intersect the start or end, but will intersect other control points. - \code{"cardinal-closed"}: a closed Cardinal spl}

\item{invalid}{(\emph{MarkConfig<>, AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig}) Defines how Vega-Lite should handle marks for invalid values (\code{null} and \code{NaN}). - If set to \code{"filter"} (default), all data items with null values will be skipped (for line, trail, and area marks) or filtered (for other marks). - If \code{null}, all data items are included. In this case, invalid values will be interpreted as zeroes.}

\item{limit}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The maximum length of the text mark in pixels. The text value will be automatically truncated if the rendered size exceeds the limit.

\strong{Default value:} \code{0} -- indicating no limit}

\item{lineBreak}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) A delimiter, such as a newline character, upon which to break text strings into multiple lines. This property is ignored if the text is array-valued.}

\item{lineHeight}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The line height in pixels (the spacing between subsequent lines of text) for multi-line text marks.}

\item{opacity}{(\emph{MarkConfig<>, AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig}) The overall opacity (value between [0,1]).

\strong{Default value:} \code{0.7} for non-aggregate plots with \code{point}, \code{tick}, \code{circle}, or \code{square} marks or layered \code{bar} charts and \code{1} otherwise.}

\item{order}{(\emph{MarkConfig<>, AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig}) For line and trail marks, this \code{order} property can be set to \code{null} or \code{false} to make the lines use the original order in the data sources.}

\item{orient}{(\emph{MarkConfig<>, AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig}) The orientation of a non-stacked bar, tick, area, and line charts. The value is either horizontal (default) or vertical. - For bar, rule and tick, this determines whether the size of the bar and tick should be applied to x or y dimension. - For area, this property determines the orient property of the Vega output. - For line and trail marks, this property determines the sort order of the points in the line if \code{config.sortLineBy} is not specified. For stacked charts, this is always determined by the orientation of the stack; therefore explicitly specified value will be ignored.}

\item{outerRadius}{(\emph{MarkConfig<>, AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig}) The outer radius in pixels of arc marks. \code{outerRadius} is an alias for \code{radius}.}

\item{padAngle}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The angular padding applied to sides of the arc, in radians.}

\item{radius}{(\emph{MarkConfig<>, AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig}) For arc mark, the primary (outer) radius in pixels.

For text marks, polar coordinate radial offset, in pixels, of the text from the origin determined by the \code{x} and \code{y} properties.}

\item{radius2}{(\emph{MarkConfig<>, AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig}) The secondary (inner) radius in pixels of arc marks.}

\item{shape}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) Shape of the point marks. Supported values include: - plotting shapes: \code{"circle"}, \code{"square"}, \code{"cross"}, \code{"diamond"}, \code{"triangle-up"}, \code{"triangle-down"}, \code{"triangle-right"}, or \code{"triangle-left"}. - the line symbol \code{"stroke"} - centered directional shapes \code{"arrow"}, \code{"wedge"}, or \code{"triangle"} - a custom \href{https://developer.mozilla.org/en-US/docs/Web/SVG/Tutorial/Paths}{SVG path string} (For correct sizing, custom shape paths should be defined within a square bounding box with coordinates ranging from -1 to 1 along both the x and y dimensions.)

\strong{Default value:} \code{"circle"}}

\item{size}{(\emph{MarkConfig<>, AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig}) Default size for marks. - For \code{point}/\code{circle}/\code{square}, this represents the pixel area of the marks. Note that this value sets the area of the symbol; the side lengths will increase with the square root of this value. - For \code{bar}, this represents the band size of the bar, in pixels. - For \code{text}, this represents the font size, in pixels.

\strong{Default value:} - \code{30} for point, circle, square marks; width/height's \code{step} - \code{2} for bar marks with discrete dimensions; - \code{5} for bar marks with continuous dimensions; - \code{11} for text marks.}

\item{smooth}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) A boolean flag (default true) indicating if the image should be smoothed when resized. If false, individual pixels should be scaled directly rather than interpolated with smoothing. For SVG rendering, this option may not work in some browsers due to lack of standardization.}

\item{startAngle}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The start angle in radians for arc marks. A value of \code{0} indicates up (north), increasing values proceed clockwise.}

\item{stroke}{(\emph{MarkConfig<>, AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig}) Default stroke color. This property has higher precedence than \code{config.color}. Set to \code{null} to remove stroke.

\strong{Default value:} (None)}

\item{strokeCap}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The stroke cap for line ending style. One of \code{"butt"}, \code{"round"}, or \code{"square"}.

\strong{Default value:} \code{"butt"}}

\item{strokeDash}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) An array of alternating stroke, space lengths for creating dashed or dotted lines.}

\item{strokeDashOffset}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The offset (in pixels) into which to begin drawing with the stroke dash array.}

\item{strokeJoin}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The stroke line join method. One of \code{"miter"}, \code{"round"} or \code{"bevel"}.

\strong{Default value:} \code{"miter"}}

\item{strokeMiterLimit}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The miter limit at which to bevel a line join.}

\item{strokeOffset}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The offset in pixels at which to draw the group stroke and fill. If unspecified, the default behavior is to dynamically offset stroked groups such that 1 pixel stroke widths align with the pixel grid.}

\item{strokeOpacity}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The stroke opacity (value between [0,1]).

\strong{Default value:} \code{1}}

\item{strokeWidth}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The stroke width, in pixels.}

\item{tension}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) Depending on the interpolation type, sets the tension parameter (for line and area marks).}

\item{text}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) Placeholder text if the \code{text} channel is not specified}

\item{theta}{(\emph{MarkConfig<>, AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig}) - For arc marks, the arc length in radians if theta2 is not specified, otherwise the start arc angle. (A value of 0 indicates up or “north”, increasing values proceed clockwise.)
\itemize{
\item For text marks, polar coordinate angle in radians.
}}

\item{theta2}{(\emph{MarkConfig<>, AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig}) The end angle of arc marks in radians. A value of 0 indicates up or “north”, increasing values proceed clockwise.}

\item{timeUnitBand}{(\emph{MarkConfig<>, AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig}) Default relative band size for a time unit. If set to \code{1}, the bandwidth of the marks will be equal to the time unit band step. If set to \code{0.5}, bandwidth of the marks will be half of the time unit band step.}

\item{timeUnitBandPosition}{(\emph{MarkConfig<>, AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig}) Default relative band position for a time unit. If set to \code{0}, the marks will be positioned at the beginning of the time unit band step. If set to \code{0.5}, the marks will be positioned in the middle of the time unit band step.}

\item{tooltip}{(\emph{MarkConfig<>, AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig}) The tooltip text string to show upon mouse hover or an object defining which fields should the tooltip be derived from.
\itemize{
\item If \code{tooltip} is \code{true} or \code{{"content": "encoding"}}, then all fields from \code{encoding} will be used. - If \code{tooltip} is \code{{"content": "data"}}, then all fields that appear in the highlighted data point will be used. - If set to \code{null} or \code{false}, then no tooltip will be used.
}

See the \href{https://vega.github.io/vega-lite/docs/tooltip.html}{\code{tooltip}} documentation for a detailed discussion about tooltip  in Vega-Lite.

\strong{Default value:} \code{null}}

\item{url}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) The URL of the image file for image marks.}

\item{width}{(\emph{AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig})

(\emph{MarkConfig<>}) Width of the marks.}

\item{x}{(\emph{MarkConfig<>, AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig}) X coordinates of the marks, or width of horizontal \code{"bar"} and \code{"area"} without specified \code{x2} or \code{width}.

The \code{value} of this channel can be a number or a string \code{"width"} for the width of the plot.}

\item{x2}{(\emph{MarkConfig<>, AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig}) X2 coordinates for ranged \code{"area"}, \code{"bar"}, \code{"rect"}, and  \code{"rule"}.

The \code{value} of this channel can be a number or a string \code{"width"} for the width of the plot.}

\item{y}{(\emph{MarkConfig<>, AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig}) Y coordinates of the marks, or height of vertical \code{"bar"} and \code{"area"} without specified \code{y2} or \code{height}.

The \code{value} of this channel can be a number or a string \code{"height"} for the height of the plot.}

\item{y2}{(\emph{MarkConfig<>, AreaConfig, BarConfig, RectConfig, LineConfig, TickConfig}) Y2 coordinates for ranged \code{"area"}, \code{"bar"}, \code{"rect"}, and  \code{"rule"}.

The \code{value} of this channel can be a number or a string \code{"height"} for the height of the plot.}

\item{line}{(\emph{AreaConfig}) A flag for overlaying line on top of area marks, or an object defining the properties of the overlayed lines.
\itemize{
\item If this value is an empty object (\code{{}}) or \code{true}, lines with default properties will be used.
\item If this value is \code{false}, no lines would be automatically added to area marks.
}

\strong{Default value:} \code{false}.}

\item{point}{(\emph{AreaConfig, LineConfig}) A flag for overlaying points on top of line or area marks, or an object defining the properties of the overlayed points.
\itemize{
\item If this property is \code{"transparent"}, transparent points will be used (for enhancing tooltips and selections).
\item If this property is an empty object (\code{{}}) or \code{true}, filled points with default properties will be used.
\item If this property is \code{false}, no points would be automatically added to line or area marks.
}

\strong{Default value:} \code{false}.}

\item{binSpacing}{(\emph{BarConfig, RectConfig}) Offset between bars for binned field. The ideal value for this is either 0 (preferred by statisticians) or 1 (Vega-Lite default, D3 example style).

\strong{Default value:} \code{1}}

\item{continuousBandSize}{(\emph{BarConfig, RectConfig}) The default size of the bars on continuous scales.

\strong{Default value:} \code{5}}

\item{cornerRadiusEnd}{(\emph{BarConfig}) - For vertical bars, top-left and top-right corner radius. - For horizontal bars, top-right and bottom-right corner radius.}

\item{discreteBandSize}{(\emph{BarConfig, RectConfig}) The default size of the bars with discrete dimensions. If unspecified, the default size is  \code{step-2}, which provides 2 pixel offset between bars.}

\item{bandSize}{(\emph{TickConfig}) The width of the ticks.

\strong{Default value:}  3/4 of step (width step for horizontal ticks and height step for vertical ticks).}

\item{thickness}{(\emph{TickConfig}) Thickness of the tick mark.

\strong{Default value:}  \code{1}}
}
\value{
A component of a Vega-Lite spec, corresponding to AnyMarkConfig definition.
}
\description{
Create object for AnyMarkConfig
}
