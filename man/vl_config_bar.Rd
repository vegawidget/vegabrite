% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzz_autogen_api.R
\name{vl_config_bar}
\alias{vl_config_bar}
\title{vl_config_bar}
\usage{
vl_config_bar(
  spec,
  .object = NULL,
  align = NULL,
  angle = NULL,
  aria = NULL,
  ariaRole = NULL,
  ariaRoleDescription = NULL,
  aspect = NULL,
  baseline = NULL,
  binSpacing = NULL,
  blend = NULL,
  color = NULL,
  continuousBandSize = NULL,
  cornerRadius = NULL,
  cornerRadiusBottomLeft = NULL,
  cornerRadiusBottomRight = NULL,
  cornerRadiusEnd = NULL,
  cornerRadiusTopLeft = NULL,
  cornerRadiusTopRight = NULL,
  cursor = NULL,
  description = NULL,
  dir = NULL,
  discreteBandSize = NULL,
  dx = NULL,
  dy = NULL,
  ellipsis = NULL,
  endAngle = NULL,
  fill = NULL,
  fillOpacity = NULL,
  filled = NULL,
  font = NULL,
  fontSize = NULL,
  fontStyle = NULL,
  fontWeight = NULL,
  height = NULL,
  href = NULL,
  innerRadius = NULL,
  interpolate = NULL,
  invalid = NULL,
  limit = NULL,
  lineBreak = NULL,
  lineHeight = NULL,
  opacity = NULL,
  order = NULL,
  orient = NULL,
  outerRadius = NULL,
  padAngle = NULL,
  radius = NULL,
  radius2 = NULL,
  shape = NULL,
  size = NULL,
  smooth = NULL,
  startAngle = NULL,
  stroke = NULL,
  strokeCap = NULL,
  strokeDash = NULL,
  strokeDashOffset = NULL,
  strokeJoin = NULL,
  strokeMiterLimit = NULL,
  strokeOffset = NULL,
  strokeOpacity = NULL,
  strokeWidth = NULL,
  tension = NULL,
  text = NULL,
  theta = NULL,
  theta2 = NULL,
  timeUnitBand = NULL,
  timeUnitBandPosition = NULL,
  tooltip = NULL,
  url = NULL,
  width = NULL,
  x = NULL,
  x2 = NULL,
  y = NULL,
  y2 = NULL
)
}
\arguments{
\item{spec}{An input vega-lite spec}

\item{.object}{Directly input an object, rather than creating one via the other arguments. Should not be used in conjunction with the other arguments other than 'spec'. Objects can be of type: BarConfig}

\item{align}{(\emph{BarConfig}) The horizontal alignment of the text or ranged marks (area, bar, image, rect, rule). One of \code{"left"}, \code{"right"}, \code{"center"}.

\strong{Note:} Expression reference is \emph{not} supported for range marks.}

\item{angle}{(\emph{BarConfig})}

\item{aria}{(\emph{BarConfig})}

\item{ariaRole}{(\emph{BarConfig})}

\item{ariaRoleDescription}{(\emph{BarConfig})}

\item{aspect}{(\emph{BarConfig})}

\item{baseline}{(\emph{BarConfig}) For text marks, the vertical text baseline. One of \code{"alphabetic"} (default), \code{"top"}, \code{"middle"}, \code{"bottom"}, \code{"line-top"}, \code{"line-bottom"}, or an expression reference that provides one of the valid values. The \code{"line-top"} and \code{"line-bottom"} values operate similarly to \code{"top"} and \code{"bottom"}, but are calculated relative to the \code{lineHeight} rather than \code{fontSize} alone.

For range marks, the vertical alignment of the marks. One of \code{"top"}, \code{"middle"}, \code{"bottom"}.

\strong{Note:} Expression reference is \emph{not} supported for range marks.}

\item{binSpacing}{(\emph{BarConfig}) Offset between bars for binned field. The ideal value for this is either 0 (preferred by statisticians) or 1 (Vega-Lite default, D3 example style).

\strong{Default value:} \code{1}}

\item{blend}{(\emph{BarConfig})}

\item{color}{(\emph{BarConfig}) Default color.

\strong{Default value:} <span style="color: #4682b4;">■</span> \code{"#4682b4"}

\strong{Note:} - This property cannot be used in a \href{https://vega.github.io/vega-lite/docs/mark.html#style-config}{style config}. - The \code{fill} and \code{stroke} properties have higher precedence than \code{color} and will override \code{color}.}

\item{continuousBandSize}{(\emph{BarConfig}) The default size of the bars on continuous scales.

\strong{Default value:} \code{5}}

\item{cornerRadius}{(\emph{BarConfig})}

\item{cornerRadiusBottomLeft}{(\emph{BarConfig})}

\item{cornerRadiusBottomRight}{(\emph{BarConfig})}

\item{cornerRadiusEnd}{(\emph{BarConfig}) - For vertical bars, top-left and top-right corner radius. - For horizontal bars, top-right and bottom-right corner radius.}

\item{cornerRadiusTopLeft}{(\emph{BarConfig})}

\item{cornerRadiusTopRight}{(\emph{BarConfig})}

\item{cursor}{(\emph{BarConfig})}

\item{description}{(\emph{BarConfig})}

\item{dir}{(\emph{BarConfig})}

\item{discreteBandSize}{(\emph{BarConfig}) The default size of the bars with discrete dimensions. If unspecified, the default size is  \code{step-2}, which provides 2 pixel offset between bars.}

\item{dx}{(\emph{BarConfig})}

\item{dy}{(\emph{BarConfig})}

\item{ellipsis}{(\emph{BarConfig})}

\item{endAngle}{(\emph{BarConfig})}

\item{fill}{(\emph{BarConfig}) Default fill color. This property has higher precedence than \code{config.color}. Set to \code{null} to remove fill.

\strong{Default value:} (None)}

\item{fillOpacity}{(\emph{BarConfig})}

\item{filled}{(\emph{BarConfig}) Whether the mark's color should be used as fill color instead of stroke color.

\strong{Default value:} \code{false} for all \code{point}, \code{line}, and \code{rule} marks as well as \code{geoshape} marks for \href{https://vega.github.io/vega-lite/docs/data.html#graticule}{\code{graticule}} data sources; otherwise, \code{true}.

\strong{Note:} This property cannot be used in a \href{https://vega.github.io/vega-lite/docs/mark.html#style-config}{style config}.}

\item{font}{(\emph{BarConfig})}

\item{fontSize}{(\emph{BarConfig})}

\item{fontStyle}{(\emph{BarConfig})}

\item{fontWeight}{(\emph{BarConfig})}

\item{height}{(\emph{BarConfig})}

\item{href}{(\emph{BarConfig})}

\item{innerRadius}{(\emph{BarConfig}) The inner radius in pixels of arc marks. \code{innerRadius} is an alias for \code{radius2}.}

\item{interpolate}{(\emph{BarConfig})}

\item{invalid}{(\emph{BarConfig}) Defines how Vega-Lite should handle marks for invalid values (\code{null} and \code{NaN}). - If set to \code{"filter"} (default), all data items with null values will be skipped (for line, trail, and area marks) or filtered (for other marks). - If \code{null}, all data items are included. In this case, invalid values will be interpreted as zeroes.}

\item{limit}{(\emph{BarConfig})}

\item{lineBreak}{(\emph{BarConfig})}

\item{lineHeight}{(\emph{BarConfig})}

\item{opacity}{(\emph{BarConfig}) The overall opacity (value between [0,1]).

\strong{Default value:} \code{0.7} for non-aggregate plots with \code{point}, \code{tick}, \code{circle}, or \code{square} marks or layered \code{bar} charts and \code{1} otherwise.}

\item{order}{(\emph{BarConfig}) For line and trail marks, this \code{order} property can be set to \code{null} or \code{false} to make the lines use the original order in the data sources.}

\item{orient}{(\emph{BarConfig}) The orientation of a non-stacked bar, tick, area, and line charts. The value is either horizontal (default) or vertical. - For bar, rule and tick, this determines whether the size of the bar and tick should be applied to x or y dimension. - For area, this property determines the orient property of the Vega output. - For line and trail marks, this property determines the sort order of the points in the line if \code{config.sortLineBy} is not specified. For stacked charts, this is always determined by the orientation of the stack; therefore explicitly specified value will be ignored.}

\item{outerRadius}{(\emph{BarConfig}) The outer radius in pixels of arc marks. \code{outerRadius} is an alias for \code{radius}.}

\item{padAngle}{(\emph{BarConfig})}

\item{radius}{(\emph{BarConfig}) For arc mark, the primary (outer) radius in pixels.

For text marks, polar coordinate radial offset, in pixels, of the text from the origin determined by the \code{x} and \code{y} properties.}

\item{radius2}{(\emph{BarConfig}) The secondary (inner) radius in pixels of arc marks.}

\item{shape}{(\emph{BarConfig})}

\item{size}{(\emph{BarConfig}) Default size for marks. - For \code{point}/\code{circle}/\code{square}, this represents the pixel area of the marks. Note that this value sets the area of the symbol; the side lengths will increase with the square root of this value. - For \code{bar}, this represents the band size of the bar, in pixels. - For \code{text}, this represents the font size, in pixels.

\strong{Default value:} - \code{30} for point, circle, square marks; width/height's \code{step} - \code{2} for bar marks with discrete dimensions; - \code{5} for bar marks with continuous dimensions; - \code{11} for text marks.}

\item{smooth}{(\emph{BarConfig})}

\item{startAngle}{(\emph{BarConfig})}

\item{stroke}{(\emph{BarConfig}) Default stroke color. This property has higher precedence than \code{config.color}. Set to \code{null} to remove stroke.

\strong{Default value:} (None)}

\item{strokeCap}{(\emph{BarConfig})}

\item{strokeDash}{(\emph{BarConfig})}

\item{strokeDashOffset}{(\emph{BarConfig})}

\item{strokeJoin}{(\emph{BarConfig})}

\item{strokeMiterLimit}{(\emph{BarConfig})}

\item{strokeOffset}{(\emph{BarConfig})}

\item{strokeOpacity}{(\emph{BarConfig})}

\item{strokeWidth}{(\emph{BarConfig})}

\item{tension}{(\emph{BarConfig})}

\item{text}{(\emph{BarConfig})}

\item{theta}{(\emph{BarConfig}) - For arc marks, the arc length in radians if theta2 is not specified, otherwise the start arc angle. (A value of 0 indicates up or “north”, increasing values proceed clockwise.)
\itemize{
\item For text marks, polar coordinate angle in radians.
}}

\item{theta2}{(\emph{BarConfig}) The end angle of arc marks in radians. A value of 0 indicates up or “north”, increasing values proceed clockwise.}

\item{timeUnitBand}{(\emph{BarConfig}) Default relative band size for a time unit. If set to \code{1}, the bandwidth of the marks will be equal to the time unit band step. If set to \code{0.5}, bandwidth of the marks will be half of the time unit band step.}

\item{timeUnitBandPosition}{(\emph{BarConfig}) Default relative band position for a time unit. If set to \code{0}, the marks will be positioned at the beginning of the time unit band step. If set to \code{0.5}, the marks will be positioned in the middle of the time unit band step.}

\item{tooltip}{(\emph{BarConfig}) The tooltip text string to show upon mouse hover or an object defining which fields should the tooltip be derived from.
\itemize{
\item If \code{tooltip} is \code{true} or \code{{"content": "encoding"}}, then all fields from \code{encoding} will be used. - If \code{tooltip} is \code{{"content": "data"}}, then all fields that appear in the highlighted data point will be used. - If set to \code{null} or \code{false}, then no tooltip will be used.
}

See the \href{https://vega.github.io/vega-lite/docs/tooltip.html}{\code{tooltip}} documentation for a detailed discussion about tooltip  in Vega-Lite.

\strong{Default value:} \code{null}}

\item{url}{(\emph{BarConfig})}

\item{width}{(\emph{BarConfig})}

\item{x}{(\emph{BarConfig}) X coordinates of the marks, or width of horizontal \code{"bar"} and \code{"area"} without specified \code{x2} or \code{width}.

The \code{value} of this channel can be a number or a string \code{"width"} for the width of the plot.}

\item{x2}{(\emph{BarConfig}) X2 coordinates for ranged \code{"area"}, \code{"bar"}, \code{"rect"}, and  \code{"rule"}.

The \code{value} of this channel can be a number or a string \code{"width"} for the width of the plot.}

\item{y}{(\emph{BarConfig}) Y coordinates of the marks, or height of vertical \code{"bar"} and \code{"area"} without specified \code{y2} or \code{height}.

The \code{value} of this channel can be a number or a string \code{"height"} for the height of the plot.}

\item{y2}{(\emph{BarConfig}) Y2 coordinates for ranged \code{"area"}, \code{"bar"}, \code{"rect"}, and  \code{"rule"}.

The \code{value} of this channel can be a number or a string \code{"height"} for the height of the plot.}
}
\value{
A modified Vega-Lite Spec
}
\description{
Add bar config (BarConfig) to a vega-lite spec.
}
