# To Do:
# Fix up other examples
# Improve way of passing selection config -- is this vl$PointSelectionConfig and documentation about how to use?
# More objects to add?
# consider collapsing add_selection_parameter and add_variable_parameter
# consider splitting add_selection_parameter to add_point_selection_parameter
# and add_interval_selection_parameter and flattening in the configs?